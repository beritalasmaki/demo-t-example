/**
 * Who is using the page. There is no session or sign-in in this app (AGENTS.md, Out of scope),
 * so this is a fixed stand-in identity — the same kind of stand-in docs/DECISIONS.md 0004
 * describes for `DecisionInput.by`. The one place it is named, so replacing it with a real
 * session later touches this file only.
 */
export const CURRENT_REVIEWER = 'Juhani Virtaleppäsoutu'
