Read `AGENTS.md` and `docs/spec-review-screen.md` before planning anything.

Propose a plan for the following task. Do not write any code yet.

The plan should name: which files will be created or changed, which existing skill(s) apply
(check `.claude/skills/`), and any judgment call you are making and why — call these out
explicitly, the way you would flag a decision worth a human's attention.

Task: $ARGUMENTS
